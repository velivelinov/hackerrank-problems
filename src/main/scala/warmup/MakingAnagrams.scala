package warmup

object MakingAnagrams extends App {

//  def makingAnagrams(s1: String, s2: String): Int = {
//    s1.toList.map(_.toString)
//      .zipAll(s2.toList.map(_.toString), "", "")
//      .fold(Map.empty[String, Int]) { (first, second) =>
//
//      }

//  }
//
//  println(makingAnagrams("abc", "cde"))

}
